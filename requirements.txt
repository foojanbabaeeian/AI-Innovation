# AI Audio Detection — minimal dependencies to run training.
#
# Install:
#   venv/Scripts/pip install -r requirements.txt
#
# The --extra-index-url below pulls CUDA-enabled torch/torchaudio from
# NVIDIA's wheel server. PyPI is still used for everything else.
# If pip ever picks the CPU wheel anyway, force it:
#   pip install torch torchaudio --index-url https://download.pytorch.org/whl/cu124 --force-reinstall

--extra-index-url https://download.pytorch.org/whl/cu124

# ── Core ML (CUDA build) ─────────────────────────────────────────────────────
torch>=2.6.0
torchaudio>=2.6.0

# ── Pretrained WavLM backbone ────────────────────────────────────────────────
transformers>=4.40.0

# ── Data & numerics ──────────────────────────────────────────────────────────
pandas>=2.0.0
numpy>=1.24.0
scikit-learn>=1.3.0
scipy>=1.11.0

# ── Audio I/O (torchaudio backend on Windows) ────────────────────────────────
soundfile>=0.12.0

# ── Config ───────────────────────────────────────────────────────────────────
pyyaml>=6.0
